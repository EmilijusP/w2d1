@model PaginationViewModel

@{
    ViewData["Title"] = "Dictionary";
}

<div class="text-center">

    <h1 class="display-4">Dictionary</h1>

    <ul>
        @foreach(var item in Model.Items)
        {
            <li>
                <a asp-controller="Home" asp-action="Index" asp-route-id="@item">@item</a>
            </li>
        }
    </ul>

    @if (Model.HasPrevious)
    {
        <a class="btn-primary" asp-controller="Words" asp-action="Index" asp-route-page="@(Model.CurrentPage - 1)">Ankstesnis</a>
    }
    @if (Model.HasNext)
    {
        <a class="btn-primary" asp-controller="Words" asp-action="Index" asp-route-page="@(Model.CurrentPage + 1)">Kitas</a>
    }

</div>
